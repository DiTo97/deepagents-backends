# Benchmark dashboard source

This Vite + React app is the source for the static benchmark site published from `benchmark/web/`.

## Commands

```bash
npm install
npm run lint
npm run build
```

The benchmark data is loaded at runtime from `benchmark/results/latest.json` in the repository and from `results/latest.json` in the deployed Pages artifact.
